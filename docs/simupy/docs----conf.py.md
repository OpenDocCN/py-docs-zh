# `.\simupy\docs\conf.py`

```
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
#
# SimuPy documentation build configuration file, created by
# sphinx-quickstart on Sun Aug  6 11:30:20 2017.
#
# This file is execfile()d with the current directory set to its
# containing dir.
#
# Note that not all possible configuration values are present in this
# autogenerated file.
#
# All configuration values have a default; values that are commented out
# serve to show the default.

# 如果扩展模块（或要使用 autodoc 自动文档的模块）在另一个目录中，
# 在此添加这些目录到 sys.path。如果目录相对于文档根目录，请使用 os.path.abspath
# 来使其绝对化，如下所示。
#
import os
import sys

sys.path.insert(0, os.path.abspath('..'))
import simupy

# -- General configuration ------------------------------------------------

# 如果你的文档需要特定版本的 Sphinx，请在这里指定。
#
# needs_sphinx = '1.0'

# 在此添加任何 Sphinx 扩展模块的名称作为字符串。它们可以是
# 与 Sphinx 一起提供的扩展（命名为 'sphinx.ext.*'）或您的自定义
# 扩展。
extensions = [
    'sphinx.ext.autodoc',
    'sphinx.ext.viewcode',
    'sphinx.ext.mathjax',
    'sphinx.ext.githubpages',
    'sphinx.ext.napoleon',
    ]

# 在这里添加包含模板的任何路径，相对于此目录。
templates_path = ['_templates']

autoclass_content = 'both'

# 源文件的后缀名。
# 您可以将多个后缀指定为字符串列表：
#
# source_suffix = ['.rst', '.md']
source_suffix = '.rst'

# 主索引文件。
master_doc = 'index'

# 关于项目的一般信息。
project = 'SimuPy'
copyright = '2015-2017, Benjamin W. L. Margolis'
author = 'Benjamin W. L. Margolis'

# 项目版本信息，用作替换 |version| 和 |release|，也用于
# 各种其他文档中的位置。
#
# 短的 X.Y 版本。
version = simupy.__version__
# 完整的版本，包括 alpha/beta/rc 标签。
release = simupy.__version__

# 生成的内容的语言。参考文档
# 查看支持的语言列表。
#
# 这也用于通过 gettext 目录进行内容翻译。
# 通常你为这些情况从命令行设置 "language"。
language = None

# 列出与源目录匹配的文件和目录的模式，用于查找源文件时要忽略。
# 这些模式还会影响 html_static_path 和 html_extra_path
exclude_patterns = ['_build', 'Thumbs.db', '.DS_Store']

# 要使用的 Pygments（语法高亮）样式名称。
pygments_style = 'sphinx'

# 如果为真，则 `todo` 和 `todoList` 产生输出，否则它们不产生任何内容。
todo_include_todos = False


# -- Options for HTML output ----------------------------------------------

# 用于 HTML 和 HTML 帮助页面的主题。请参阅文档
# 有关内置主题的列表。
#
html_theme = 'sphinx_rtd_theme'
html_theme_options = {
    'logo_only': True


# 设置一个键为'logo_only'，对应的值为True的布尔类型变量
# Custom sidebar templates, must be a dictionary that maps document names
# to template names.
#
# This is required for the alabaster theme
# refs: http://alabaster.readthedocs.io/en/latest/installation.html#sidebars
# 定义自定义侧边栏模板，必须是一个将文档名称映射到模板名称的字典。
# 这对于 alabaster 主题是必需的。
# 参考链接: http://alabaster.readthedocs.io/en/latest/installation.html#sidebars
html_sidebars = {
    '**': [
        'about.html',     # 显示关于页面
        'navigation.html',    # 显示导航链接
        'relations.html',     # 需要 'show_related': True 主题选项来显示相关链接
        'searchbox.html',     # 显示搜索框
        'donate.html',        # 显示捐赠链接
    ]
}

# Output file base name for HTML help builder.
# HTML 帮助生成器的输出文件基本名称。
htmlhelp_basename = 'SimuPydoc'

# Grouping the document tree into LaTeX files. List of tuples
# (source start file, target name, title,
#  author, documentclass [howto, manual, or own class]).
# 将文档树分组到 LaTeX 文件中。元组列表
# (源文件起始位置，目标文件名，标题，
#  作者，文档类[howto, manual, or own class])
latex_documents = [
    (master_doc, 'SimuPy.tex', 'SimuPy Documentation',
     'Benjamin W. L. Margolis', 'manual'),
]

# One entry per manual page. List of tuples
# (source start file, name, description, authors, manual section).
# 每个手册页面的条目。元组列表
# (源文件起始位置，名称，描述，作者，手册章节)
man_pages = [
    (master_doc, 'simupy', 'SimuPy Documentation',
     [author], 1)
]

# Grouping the document tree into Texinfo files. List of tuples
# (source start file, target name, title, author,
#  dir menu entry, description, category)
# 将文档树分组到 Texinfo 文件中。元组列表
# (源文件起始位置，目标文件名，标题，作者，
#  目录菜单条目，描述，类别)
texinfo_documents = [
    (master_doc, 'SimuPy', 'SimuPy Documentation',
     author, 'SimuPy', 'One line description of project.',
     'Miscellaneous'),
]
```