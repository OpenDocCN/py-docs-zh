- en: jax.extend.ffi module
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: jax.extend.ffi 模块
- en: 原文：[`jax.readthedocs.io/en/latest/jax.extend.ffi.html`](https://jax.readthedocs.io/en/latest/jax.extend.ffi.html)
  id: totrans-1
  prefs:
  - PREF_BQ
  type: TYPE_NORMAL
  zh: 原文：[`jax.readthedocs.io/en/latest/jax.extend.ffi.html`](https://jax.readthedocs.io/en/latest/jax.extend.ffi.html)
- en: '| `ffi_lowering`(call_target_name, *[, ...]) | Build a lowering rule for an
    foreign function interface (FFI) target. |'
  id: totrans-2
  prefs: []
  type: TYPE_TB
  zh: '| `ffi_lowering`(call_target_name, *[, ...]) | 构建一个外部函数接口（FFI）目标的降低规则。 |'
- en: '| `pycapsule`(funcptr) | Wrap a ctypes function pointer in a PyCapsule. |'
  id: totrans-3
  prefs: []
  type: TYPE_TB
  zh: '| `pycapsule`(funcptr) | 将一个 ctypes 函数指针包装在 PyCapsule 中。 |'
