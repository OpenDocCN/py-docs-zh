- en: jax.dtypes module
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: 原文：[`jax.readthedocs.io/en/latest/jax.dtypes.html`](https://jax.readthedocs.io/en/latest/jax.dtypes.html)
  prefs:
  - PREF_BQ
  type: TYPE_NORMAL
- en: '| `bfloat16` | bfloat16 floating-point values |'
  prefs: []
  type: TYPE_TB
- en: '| `canonicalize_dtype`(dtype[, allow_extended_dtype]) | Convert from a dtype
    to a canonical dtype based on config.x64_enabled. |'
  prefs: []
  type: TYPE_TB
- en: '| `float0` | DType class corresponding to the scalar type and dtype of the
    same name. |'
  prefs: []
  type: TYPE_TB
- en: '| `issubdtype`(a, b) | Returns True if first argument is a typecode lower/equal
    in type hierarchy. |'
  prefs: []
  type: TYPE_TB
- en: '| `prng_key`() | Scalar class for PRNG Key dtypes. |'
  prefs: []
  type: TYPE_TB
- en: '| `result_type`(*args[, return_weak_type_flag]) | Convenience function to apply
    JAX argument dtype promotion. |'
  prefs: []
  type: TYPE_TB
- en: '| `scalar_type_of`(x) | Return the scalar type associated with a JAX value.
    |'
  prefs: []
  type: TYPE_TB
