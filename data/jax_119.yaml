- en: jax.dlpack module
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: jax.dlpack 模块
- en: 原文：[`jax.readthedocs.io/en/latest/jax.dlpack.html`](https://jax.readthedocs.io/en/latest/jax.dlpack.html)
  id: totrans-1
  prefs:
  - PREF_BQ
  type: TYPE_NORMAL
  zh: 原文：[`jax.readthedocs.io/en/latest/jax.dlpack.html`](https://jax.readthedocs.io/en/latest/jax.dlpack.html)
- en: '| `from_dlpack`(external_array[, device, copy]) | Returns a `Array` representation
    of a DLPack tensor. |'
  id: totrans-2
  prefs: []
  type: TYPE_TB
  zh: '| `from_dlpack`(external_array[, device, copy]) | 返回一个 DLPack 张量的 `Array` 表示形式。
    |'
- en: '| `to_dlpack`(x[, stream, src_device, ...]) | Returns a DLPack tensor that
    encapsulates a `Array` `x`. |'
  id: totrans-3
  prefs: []
  type: TYPE_TB
  zh: '| `to_dlpack`(x[, stream, src_device, ...]) | 返回一个封装了 `Array` `x` 的 DLPack
    张量。 |'
