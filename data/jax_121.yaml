- en: jax.dtypes module
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: jax.dtypes 模块
- en: 原文：[`jax.readthedocs.io/en/latest/jax.dtypes.html`](https://jax.readthedocs.io/en/latest/jax.dtypes.html)
  id: totrans-1
  prefs:
  - PREF_BQ
  type: TYPE_NORMAL
  zh: 原文：[`jax.readthedocs.io/en/latest/jax.dtypes.html`](https://jax.readthedocs.io/en/latest/jax.dtypes.html)
- en: '| `bfloat16` | bfloat16 floating-point values |'
  id: totrans-2
  prefs: []
  type: TYPE_TB
  zh: '| `bfloat16` | bfloat16 浮点数值 |'
- en: '| `canonicalize_dtype`(dtype[, allow_extended_dtype]) | Convert from a dtype
    to a canonical dtype based on config.x64_enabled. |'
  id: totrans-3
  prefs: []
  type: TYPE_TB
  zh: '| `canonicalize_dtype`(dtype[, allow_extended_dtype]) | 根据`config.x64_enabled`配置将dtype转换为规范的dtype。
    |'
- en: '| `float0` | DType class corresponding to the scalar type and dtype of the
    same name. |'
  id: totrans-4
  prefs: []
  type: TYPE_TB
  zh: '| `float0` | 对应于相同名称的标量类型和dtype的DType类。 |'
- en: '| `issubdtype`(a, b) | Returns True if first argument is a typecode lower/equal
    in type hierarchy. |'
  id: totrans-5
  prefs: []
  type: TYPE_TB
  zh: '| `issubdtype`(a, b) | 如果第一个参数是类型代码在类型层次结构中较低/相等，则返回True。 |'
- en: '| `prng_key`() | Scalar class for PRNG Key dtypes. |'
  id: totrans-6
  prefs: []
  type: TYPE_TB
  zh: '| `prng_key`() | PRNG Key dtypes 的标量类。 |'
- en: '| `result_type`(*args[, return_weak_type_flag]) | Convenience function to apply
    JAX argument dtype promotion. |'
  id: totrans-7
  prefs: []
  type: TYPE_TB
  zh: '| `result_type`(*args[, return_weak_type_flag]) | 方便函数，用于应用 JAX 参数dtype提升。
    |'
- en: '| `scalar_type_of`(x) | Return the scalar type associated with a JAX value.
    |'
  id: totrans-8
  prefs: []
  type: TYPE_TB
  zh: '| `scalar_type_of`(x) | 返回与JAX值关联的标量类型。 |'
